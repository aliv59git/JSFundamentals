
var temp;
jsConsole.writeLine(temp);
var tmp1 = null;
jsConsole.writeLine(tmp1);
var obj = {};
jsConsole.writeLine(obj.name);
jsConsole.writeLine(typeof (obj.name));
jsConsole.writeLine(temp / tmp1);
jsConsole.writeLine(typeof(temp / tmp1));
jsConsole.writeLine(obj.name / 0);
jsConsole.writeLine(typeof (undefined));
jsConsole.writeLine(typeof (null));
var numb = parseInt(temp);
jsConsole.writeLine(numb);



